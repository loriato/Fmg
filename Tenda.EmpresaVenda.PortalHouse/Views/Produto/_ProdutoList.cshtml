@using Europa.Extensions
@model List<Tenda.Domain.EmpresaVenda.Models.Views.ViewProduto>

@if (!Model.IsEmpty())
{
    foreach (var item in Model)
    {
        {
            Html.RenderPartial("_ProdutoListItem", item);
        }
    }
}
