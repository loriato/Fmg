@using Tenda.EmpresaVenda.Web.Models.Application
@using Tenda.Domain.Core.Enums;
@using Europa.Resources

<div class="col-md-24">
    <form id="form_filtro_empresa_venda">
        <div class="col-md-6">
            @Html.Label(GlobalMessages.EmpresaVendas)
            @Html.TextBox("filtro_empresa_venda", "", new { @class = "form-control", maxlength = 128 })
        </div>
        <div class="col-md-3 col-md-offset-1">
            @Html.Label(GlobalMessages.Cnpj)
            @Html.TextBox("filtro_cnpj", "", new { @class = "form-control", maxlength = 20 })
        </div>
        <div class="col-md-3 col-md-offset-1">
            @Html.Label(GlobalMessages.Creci)
            @Html.TextBox("filtro_creci", "", new { @class = "form-control", maxlength = 20 })
        </div>
        <div class="pull-right" style="margin-top: 22px">
            <button class="btn btn-default" title="@GlobalMessages.Filtrar" onClick="Europa.Controllers.NovaEv.Filtrar()"><i class="fa fa-filter"></i> @GlobalMessages.Filtrar</button>
            <button type="button" title="@GlobalMessages.Limpar" class="btn btn-default" onClick="Europa.Controllers.NovaEv.LimparFiltro()"><i class="fa fa-eraser"></i> @GlobalMessages.Limpar</button>
        </div>
    </form>

    <div ng-controller="listaEmpresas as table">
        <div id="datatable_header" class="col-md-24">
            <div class="col-md-6" style="margin-top:5px">
                @Html.Label(@GlobalMessages.EmpresaVendas)
            </div>
        </div>
        <table datatable="" dt-options="table.dtOptions" dt-columns="table.dtColumns"
               dt-instance="table.dtInstance" class="table table-striped table-bordered"
               dt-column-defs="table.dtColumnDefs"></table>
    </div>
</div>

<form id="Exportar"></form>